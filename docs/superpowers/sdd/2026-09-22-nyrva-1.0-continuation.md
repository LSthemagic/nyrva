# Continuação da execução — Nyrva 1.0

Plano: `docs/superpowers/plans/2026-09-22-nyrva-1.0.md`
Spec: `docs/superpowers/specs/2026-09-22-nyrva-1.0-design.md`
Branch: `feat/nyrva-1.0`; main preservada em `abc1da40`.

## Recuperação

O repositório agora é independente (`fork: false`). O último estado remoto antes desta continuação era `072c045a`, contendo o core anterior e kits de dependências. O dashboard mencionado na tentativa interrompida não está nessa árvore. Não foi reaproveitado nem declarado concluído sem código verificável.

O runtime local começou a falhar com ClientError inclusive em comandos vazios. A execução segue por alterações atômicas na branch e testes GitHub Actions; nenhuma verificação local nova é alegada.

## Core: sessões independentes e statusline Claude

- RED: `4c0332fa`, run `35803236973`, job Linux `106998135938`. Compilação bem-sucedida, 25 contratos anteriores verdes, sete contratos novos executados e falhando pelo comportamento ausente.
- Novos contratos: statusline documentada Claude (quota/contexto/custo estimado/effort); identidade conversation_id do Antigravity; sessões simultâneas no mesmo alias e timestamp; metadados recentes independentes da recuperação de quota; recusa de custo inválido; percentuais inválidos; migração v1 preservando observações.
- Implementação: schema SQLite v2 acrescenta identidade de sessão à chave, migração transacional e consulta de sessões independente. Snapshot JSON v1 recebe campo opcional metrics compatível com leituras antigas. Custos são cumulativos e estimados, não cobrança.
- GREEN ainda pendente até consultar os logs do commit de implementação.

## Regras de continuidade

Os três macrosteps permanecem abertos até seus critérios serem verificados. Não usar `unsupported` para esconder código faltante. Não publicar stable, mover main, remover atribuições ou modificar credenciais para obter aceite artificial. CI não comprova sessão autenticada real de provider.

Referências consultadas: documentação oficial `https://code.claude.com/docs/en/statusline` e `https://antigravity.google/docs/cli/statusline/`.

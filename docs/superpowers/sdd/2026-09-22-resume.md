# Resume ledger — Nyrva 1.0

## Current checkpoint: 2026-09-23

**Read `docs/superpowers/sdd/2026-09-23-experience.md` first.** It supersedes the task states in the historical section below.

Current user instruction: **one macrostep at a time**. This execution worked only on Macrostep 2 — Experience. Eight code/test commits were published on `feat/nyrva-1.0`, ending at `2ceed852cdf78929c8a623f3eb186b8d68e066f1`, followed by the documentation checkpoint containing this file. Main was not modified.

Experience now has a dashboard, native scoped data bridge, history, sessions/projects/agents, local alerts, layouts, privacy/preferences and reproducible tests. Presentation contracts, facade contracts, browser E2E, both-platform compilation/regressions and Linux native WebKit/IPC/SQLite smoke passed. **Experience acceptance remains open:** the Windows graphical smoke failed at startup, and an attempted correction to synchronous tray-window creation was blocked by the platform and is not in the code. See the current ledger for exact job IDs, evidence, limitations and the next bounded work.

**Do not start Everywhere, merge, tag or release from this checkpoint.** Do not reset the feature branch to main or redo the completed UI/facade work. The full workspace is not claimed green; the four pre-existing Everywhere contract failures were preserved.

## Historical checkpoint retained for traceability

Plan: `docs/superpowers/plans/2026-09-22-nyrva-1.0.md`.
Spec: `docs/superpowers/specs/2026-09-22-nyrva-1.0-design.md`.
Historical resume base: `f9acc68cd561a23bf7bf35eb463556b4d0be7c62` on `feat/nyrva-1.0`; main was `abc1da40fbe63bbbbc381024d66bb71c63dec571`.

At that earlier checkpoint, live GitHub reads showed seven additional session/verification/contract commits on the implementation branch. They needed to be preserved, not reset to main.

Historical RED: Actions run `35804301017`, Linux job `107001521716`: 32 existing contracts passed and all 8 cockpit contracts failed because their commands were not yet implemented. This is historical evidence, **not the current Experience backend state**.

Pre-flight rules retained: UI/CLI/API consume the same sanitized core view; ingestion paths use shared privacy settings. Integration receipts are private local state, not portable settings or diagnostic exports. Alert deduplication is transactional across CLI/desktop processes. Quota lifetime and session lifetime stay independent.

Ruling retained: use the newer feature-branch head to preserve prior work. Use actual CI evidence while local execution is unavailable, and never count compilation or a mocked browser as full native acceptance.

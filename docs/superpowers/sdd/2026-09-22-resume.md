# Resume ledger — Nyrva 1.0

Plan: `docs/superpowers/plans/2026-09-22-nyrva-1.0.md`.
Spec: `docs/superpowers/specs/2026-09-22-nyrva-1.0-design.md`.
Resume base: `f9acc68cd561a23bf7bf35eb463556b4d0be7c62` on `feat/nyrva-1.0`; main remains `abc1da40fbe63bbbbc381024d66bb71c63dec571`.

The previous response's claim that both branches were aligned is stale. Live GitHub reads show seven additional session/verification/contract commits on the implementation branch. Preserve them; do not reset to main.

Observed RED: Actions run 35804301017, Linux job 107001521716: 32 existing contracts passed, all 8 cockpit contracts failed because their commands are not implemented. This is the actual interruption point.

Execution: same approved three macrosteps, no merge, no tag, no automatic publication. Container execution currently returns ClientError; implement through GitHub Git trees and verify on Windows/Linux CI. The native verification kit was retrieved but has NOT been executed in this session.

Pre-flight: UI/CLI/API consume the same sanitized core view; all ingestion paths must use shared privacy settings. Integration receipts are private local state, not portable settings or diagnostic exports. Alert deduplication must be transactional across CLI/desktop processes. Quota lifetime and session lifetime stay independent.

Ruling: continue from the newer feature-branch head, not the older recalled checkpoint — prevents discarding verified session work; cost if wrong: changes remain isolated on the feature branch.
Ruling: use real CI execution while the local terminal is unavailable — no test is marked passed from inspection; cost: native evidence may remain blocked if the runners cannot exercise the required desktop behavior.

## Task state

- Core/session foundation: existing verified baseline, regression suite retained.
- Experience backend: eight existing RED contracts; implementation pending.
- Everywhere: added filesystem/process acceptance contracts before handlers; implementation pending.
- Dashboard/native desktop: pending.
- Release 1.0: BLOCKED until actual acceptance evidence is complete.

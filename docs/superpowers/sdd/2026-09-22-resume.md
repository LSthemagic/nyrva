# Resume ledger — Nyrva 1.0

## Current checkpoint: Experience completed, 2026-09-23

Read **`docs/superpowers/sdd/2026-09-23-experience-close.md` first**. It supersedes the earlier resume and open-Experience task states.

User scope remains **one macrostep at a time**. PR #18 was merged into main at `fe1e88f2517d856dee899390e51709226a6ec92d`. The subsequent Experience corrections are on `fix/experience-completion`, with verified code commit `fbe86ec61e87cdb1074a2837492e3f98153344cc` and documentation-only successors.

Macrostep 2 implementation and scoped automated acceptance are complete: presentation/browser, facade, Windows/Linux desktop regressions, and actual native WebView2/WebKit + Tauri + SQLite checks passed in run `35861118995`. Both prior Windows findings are closed. The latest ledger records root causes, test-first evidence, job IDs, artifacts and review limits.

**This is not full-project/release acceptance.** Four pre-existing Everywhere contracts still fail on both platforms; no tests were suppressed. Native CI does not replace physical multi-monitor/DPI, OS-tray mouse interaction, installed-package or real-provider acceptance. Those remain part of final release work.

No main merge, tag or publication was performed by this completion task. Before resuming, read current remote branch/PR state and preserve the completion changes. Do not reset to the old `feat/nyrva-1.0` branch, redo the UI/facade, or begin Everywhere without a separate instruction.

## Historical checkpoints

The earlier resume ledger is preserved in Git at commit `36c7c44af7f691a8739a48973f3793da4dd2538c`. Its statements that the Experience backend or Windows acceptance remain pending are historical, not current.

Standing constraints: all views consume sanitized shared-core contracts; provider credentials remain read-only; quotas and session lifetimes remain independent; no combined incompatible quota percentage, invented billing or inferred agent relationships. Version 1.0 requires completion of all three macrosteps and final platform/package acceptance.

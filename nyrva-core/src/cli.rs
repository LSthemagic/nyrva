use std::{io::{Read, Write}, path::Path};
pub fn run(_args: &[String], _root: &Path, _input: &mut dyn Read, _out: &mut dyn Write) -> Result<(), String> { Err("telemetry commands not initialized".into()) }
